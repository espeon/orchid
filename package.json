{
  "name": "orchid",
  "private": true,
  "scripts": {
    "dev": "cd orchid-web && bun run dev",
    "build": "cd orchid-web && bun run build",
    "preview": "cd orchid-web && bun run preview"
  },
  "packageManager": "pnpm@9.15.2",
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
